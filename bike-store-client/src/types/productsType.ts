export type TProduct = {
    _id: string
    name: string
    brand: string
    image: string
    model: string
    price: number
    category: string
    description: string
    quantity: number
    inStock: boolean
    createdAt: string
    updatedAt: string
}